name: Auto Review

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  auto-review:
    runs-on: ubuntu-latest

    steps:
    - name: Auto Review
      uses: Dvs-git/calcom@v1.2.4
      with:
        reviewers: Dvs-git
